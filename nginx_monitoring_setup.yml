- name: Deploy Nginx and Monitor Resources
  hosts: all
  become: yes
  tasks:

    - name: Install Nginx
      ansible.builtin.yum:
        name: nginx
        state: present

    - name: Start and enable Nginx
      ansible.builtin.systemd:
        name: nginx
        state: started
        enabled: yes

    - name: Create directory for HTML files
      ansible.builtin.file:
        path: /var/www/html
        state: directory
        mode: '0755'

    - name: Gather CPU usage
      ansible.builtin.shell: top -bn1 | grep 'Cpu(s)' | awk '{print $2 + $4}'
      register: cpu_usage
      changed_when: false

    - name: Gather RAM usage
      ansible.builtin.shell: free -m | awk 'NR==2{print $3}'
      register: ram_usage
      changed_when: false

    - name: Gather Disk usage
      ansible.builtin.shell: df -h | awk '$NF=="/" {print $5}'
      register: disk_usage
      changed_when: false

    - name: Create HTML report
      ansible.builtin.template:
        src: index.html.j2
        dest: /var/www/html/index.html
      vars:
        cpu_usage_value: "{{ cpu_usage }}%"
        ram_usage_value: "{{ ram_usage }} MB"
        disk_usage_value: "{{ disk_usage }}"
